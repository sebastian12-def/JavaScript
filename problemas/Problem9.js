// Declaramos dos variables
let a = 5;
let b = 10;

// Intercambiamos sus valores usando una variable auxiliar
let temp = a;
a = b;
b = temp;

// Mostramos los valores después del intercambio
console.log("a: " + a);
console.log("b: " + b);
